services:

  hi-bpt-mongo:
    image: registry.diginfra.net/tsd/hi-bataafse-politieke-tijdschriften-mongo:0.0.2
    ports:
      - '27017:27017'
    expose:
      - '27017'
    volumes:
      - hi-bpt-mongo-data:/data/db
    healthcheck:
      test: [ "CMD-SHELL", "mongosh --quiet --eval 'db.adminCommand({ ping: 1 })' > /dev/null" ]
      interval: 5s
      timeout: 5s
      retries: 30
    restart: unless-stopped
    networks:
      - hi-bpt-net

  hi-bpt-elastic:
    image: docker.elastic.co/elasticsearch/elasticsearch:9.4.2
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
    ports:
      - '9200:9200'
      - '9300:9300'
    expose:
      - '9200'
      - '9300'
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsS http://localhost:9200 >/dev/null" ]
      interval: 5s
      timeout: 5s
      retries: 60
    volumes:
      - hi-bpt-es-data:/usr/share/elasticsearch/data
    restart: unless-stopped
    networks:
      - hi-bpt-net

  hi-bpt-panoptes:
    image: registry.diginfra.net/tsd/panoptes-backend:hi-bataafse-politieke-tijdschriften
    environment:
      - ES_SCHEME=http
      - ES_HOST=hi-bpt-elastic
      - ES_PORT=9200
      - MONGO_CONNECTION=mongodb://hi-bpt-mongo:27017/
    depends_on:
      hi-bpt-mongo:
        condition: service_healthy
      hi-bpt-elastic:
        condition: service_healthy
    ports:
      - '8000:8000'
    expose:
      - '8000'
    restart: unless-stopped
    networks:
      - hi-bpt-net

  hi-bpt-panoptes-browser:
    image: registry.diginfra.net/tsd/hi-bataafse-politieke-tijdschriften-browser:0.0.2
    environment:
      - VITE_PANOPTES_URL=http://localhost:8000
      - VITE_PANOPTES_IS_EMBEDDED=false
      - VITE_PANOPTES_DATASET=politieke-tijdschriften
      - VITE_PANOPTES_SEARCH_PATH=/$$dataset/search
      - VITE_PANOPTES_DETAIL_PATH=/$$dataset/details/$$id
    ports:
      - "80:80"
    expose:
      - "80"
    restart: unless-stopped
    networks:
      - hi-bpt-net

  hi-bpt-indexer:
    image: registry.diginfra.net/tsd/hi-bataafse-politieke-tijdschriften-indexer:0.0.2
    environment:
      - ES_HOST=hi-bpt-elastic
      - ES_PORT=9200
    depends_on:
      hi-bpt-elastic:
        condition: service_healthy
    networks:
      - hi-bpt-net

volumes:
  hi-bpt-es-data:
  hi-bpt-mongo-data:

networks:
  hi-bpt-net: